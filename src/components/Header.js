import React from 'react';
import './Header.css'; // optional if you want custom styling

const Header = () => {
  return (
    <header className="header">
      <h1>💸 Expense Tracker</h1>
    </header>
  );
};

export default Header;
